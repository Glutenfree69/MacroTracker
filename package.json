{
  "name": "macrotracker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run data && vite",
    "build": "npm run data && tsc --noEmit && vite build",
    "preview": "vite preview",
    "data": "node scripts/build-ingredients.mjs",
    "check": "node scripts/build-ingredients.mjs --check"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.50.1-build1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "yaml": "^2.8.1"
  }
}
